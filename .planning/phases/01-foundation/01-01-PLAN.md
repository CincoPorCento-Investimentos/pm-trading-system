---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - hft-synthetic-monitoring/pom.xml
  - hft-synthetic-monitoring/src/main/java/com/cryptohft/monitoring/SyntheticMonitoringApplication.java
  - hft-synthetic-monitoring/src/main/resources/application.yml
  - hft-synthetic-monitoring/src/test/java/com/cryptohft/monitoring/SyntheticMonitoringApplicationTest.java
autonomous: true
requirements:
  - CORE-01

must_haves:
  truths:
    - "hft-synthetic-monitoring aparece como module no root pom.xml"
    - "mvn clean compile -pl hft-synthetic-monitoring compila sem erros"
    - "SyntheticMonitoringApplication sobe na porta 8081 com Spring Boot"
    - "GET /actuator/health retorna HTTP 200 com status UP"
  artifacts:
    - path: "pom.xml"
      provides: "Module declaration e dependencyManagement entry para hft-synthetic-monitoring"
      contains: "hft-synthetic-monitoring"
    - path: "hft-synthetic-monitoring/pom.xml"
      provides: "Maven module com deps Spring Boot, actuator, micrometer, Java-WebSocket, lombok"
      contains: "spring-boot-maven-plugin"
    - path: "hft-synthetic-monitoring/src/main/java/com/cryptohft/monitoring/SyntheticMonitoringApplication.java"
      provides: "Entry point Spring Boot com @EnableScheduling e @EnableConfigurationProperties"
      contains: "@SpringBootApplication"
    - path: "hft-synthetic-monitoring/src/main/resources/application.yml"
      provides: "Configuracao server.port=8081, actuator endpoints, synthetic-monitoring config block"
      contains: "server.port: 8081"
    - path: "hft-synthetic-monitoring/src/test/java/com/cryptohft/monitoring/SyntheticMonitoringApplicationTest.java"
      provides: "Smoke test de contexto Spring Boot e actuator health"
      contains: "@SpringBootTest"
  key_links:
    - from: "hft-synthetic-monitoring/pom.xml"
      to: "pom.xml"
      via: "parent declaration + module entry"
      pattern: "<module>hft-synthetic-monitoring</module>"
    - from: "SyntheticMonitoringApplication.java"
      to: "application.yml"
      via: "Spring Boot auto-configuration"
      pattern: "@SpringBootApplication"
---

<objective>
Scaffold do modulo Maven `hft-synthetic-monitoring` com entry point Spring Boot funcional, actuator configurado e testes de smoke.

Purpose: Criar a fundacao do modulo para que Plans 01-02 e 01-03 possam adicionar models e configuration sem se preocupar com build system ou application bootstrap.
Output: Modulo Maven compilavel, app Spring Boot rodando na porta 8081, actuator respondendo em /actuator/health.
</objective>

<execution_context>
@/Users/devcaos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/devcaos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md

Reference files for patterns:
@pom.xml (root — module list at line 22, dependencyManagement at line 64)
@hft-app/pom.xml (spring-boot-maven-plugin pattern at line 82)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hft-synthetic-monitoring Maven module and register in root pom</name>
  <files>
    pom.xml
    hft-synthetic-monitoring/pom.xml
  </files>
  <action>
    1. Edit root `pom.xml`:
       - Line 31 (after `<module>hft-app</module>`): ADD `<module>hft-synthetic-monitoring</module>`
       - Line 106 area (after hft-persistence dependencyManagement entry): ADD dependencyManagement entry:
         ```xml
         <dependency>
             <groupId>com.cryptohft</groupId>
             <artifactId>hft-synthetic-monitoring</artifactId>
             <version>${project.version}</version>
         </dependency>
         ```

    2. CREATE `hft-synthetic-monitoring/pom.xml`:
       - parent: com.cryptohft:crypto-hft-platform:1.0.0-SNAPSHOT
       - artifactId: hft-synthetic-monitoring
       - name: HFT Synthetic Monitoring
       - description: Synthetic monitoring companion for crypto-hft-platform
       - packaging: jar
       - Dependencies (NO version tags — inherited from parent):
         - spring-boot-starter-web
         - spring-boot-starter-actuator
         - micrometer-registry-prometheus (scope runtime)
         - org.java-websocket:Java-WebSocket (version from parent: ${java-websocket.version})
         - lombok (scope provided)
         - spring-boot-starter-test (scope test)
       - Build plugins section — mirror hft-app/pom.xml pattern:
         ```xml
         <plugin>
             <groupId>org.springframework.boot</groupId>
             <artifactId>spring-boot-maven-plugin</artifactId>
             <configuration>
                 <mainClass>com.cryptohft.monitoring.SyntheticMonitoringApplication</mainClass>
                 <layers>
                     <enabled>true</enabled>
                 </layers>
             </configuration>
             <executions>
                 <execution>
                     <goals>
                         <goal>repackage</goal>
                     </goals>
                 </execution>
             </executions>
         </plugin>
         ```
       - ZERO deps internas (nenhum hft-common, hft-api, etc.) — modulo completamente independente
  </action>
  <verify>
    <automated>cd /Users/devcaos/conductor/workspaces/pm-trading-system/walla-walla && mvn clean compile -pl hft-synthetic-monitoring -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - hft-synthetic-monitoring aparece em root pom.xml modules e dependencyManagement
    - hft-synthetic-monitoring/pom.xml existe com todas as deps e spring-boot-maven-plugin
    - `mvn clean compile -pl hft-synthetic-monitoring` compila com BUILD SUCCESS
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Spring Boot application entry point, configuration and smoke tests</name>
  <files>
    hft-synthetic-monitoring/src/main/java/com/cryptohft/monitoring/SyntheticMonitoringApplication.java
    hft-synthetic-monitoring/src/main/resources/application.yml
    hft-synthetic-monitoring/src/test/java/com/cryptohft/monitoring/SyntheticMonitoringApplicationTest.java
  </files>
  <action>
    1. CREATE `SyntheticMonitoringApplication.java`:
       - Package: com.cryptohft.monitoring
       - Annotations: @SpringBootApplication, @EnableScheduling, @Slf4j
       - NOTE: @EnableConfigurationProperties(MonitoringProperties.class) will be added in Plan 01-03 when MonitoringProperties is created. For now, just @SpringBootApplication + @EnableScheduling.
       - Standard main method with SpringApplication.run
       - Log startup message: "HFT Synthetic Monitoring started on port {}"

    2. CREATE `application.yml`:
       ```yaml
       server:
         port: 8081

       management:
         endpoints:
           web:
             exposure:
               include: health,info,metrics,prometheus
         endpoint:
           health:
             show-details: always

       # Synthetic monitoring configuration — bound to MonitoringProperties (Plan 01-03)
       synthetic-monitoring:
         interval-ms: 30000
         timeout-ms: 3000
         target:
           base-url: http://localhost:8080
           ws-url: ws://localhost:8080/ws/trading
         order:
           symbol: BTCUSD
           price: 100.00
           quantity: 0.001
           account: SM_CHECK
           client-id-prefix: sm-
         alert:
           consecutive-failures-threshold: 3
           warn-threshold: 5
           webhook-url: ""
       ```

    3. CREATE `SyntheticMonitoringApplicationTest.java`:
       - Package: com.cryptohft.monitoring
       - @SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
       - @Autowired TestRestTemplate restTemplate
       - Test 1: `contextLoads()` — verifica que o contexto Spring sobe sem erro (body vazio, so nao lancar exception)
       - Test 2: `actuatorHealthRespondsUp()` — GET /actuator/health, assert status 200, assert body contem "UP"
  </action>
  <verify>
    <automated>cd /Users/devcaos/conductor/workspaces/pm-trading-system/walla-walla && mvn test -pl hft-synthetic-monitoring -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    - SyntheticMonitoringApplication.java existe com @SpringBootApplication e @EnableScheduling
    - application.yml configura porta 8081, actuator endpoints e todo o bloco synthetic-monitoring
    - Ambos os testes passam: contextLoads e actuatorHealthRespondsUp
    - App sobe na porta 8081 e /actuator/health retorna UP
  </done>
</task>

</tasks>

<verification>
1. `mvn clean compile -pl hft-synthetic-monitoring` — BUILD SUCCESS
2. `mvn test -pl hft-synthetic-monitoring` — 2 tests passam
3. Root pom.xml contem `<module>hft-synthetic-monitoring</module>`
4. Root pom.xml contem dependencyManagement entry para hft-synthetic-monitoring
5. hft-synthetic-monitoring/pom.xml nao referencia nenhum modulo interno (zero deps internas)
</verification>

<success_criteria>
- Modulo Maven funcional que compila isoladamente
- Spring Boot app sobe na porta 8081
- /actuator/health retorna HTTP 200 com status UP
- /actuator/prometheus acessivel (micrometer-registry-prometheus configurado)
- Testes de smoke passam
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
