---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - hft-synthetic-monitoring/src/main/java/com/cryptohft/monitoring/config/MonitoringProperties.java
  - hft-synthetic-monitoring/src/main/java/com/cryptohft/monitoring/SyntheticMonitoringApplication.java
  - hft-synthetic-monitoring/src/test/java/com/cryptohft/monitoring/config/MonitoringPropertiesTest.java
autonomous: true
requirements:
  - CORE-02

must_haves:
  truths:
    - "MonitoringProperties carrega todos os parametros do application.yml sob synthetic-monitoring.*"
    - "target.baseUrl resolve para http://localhost:8080 (default do yml)"
    - "order.account resolve para SM_CHECK e order.clientIdPrefix resolve para sm-"
    - "alert.consecutiveFailuresThreshold resolve para 3 e alert.warnThreshold resolve para 5"
    - "intervalMs default e 30000 e timeoutMs default e 3000"
    - "@EnableConfigurationProperties(MonitoringProperties.class) esta no SyntheticMonitoringApplication"
  artifacts:
    - path: "hft-synthetic-monitoring/src/main/java/com/cryptohft/monitoring/config/MonitoringProperties.java"
      provides: "@ConfigurationProperties com nested Target, Order, Alert classes"
      contains: "@ConfigurationProperties"
    - path: "hft-synthetic-monitoring/src/main/java/com/cryptohft/monitoring/SyntheticMonitoringApplication.java"
      provides: "Entry point atualizado com @EnableConfigurationProperties"
      contains: "@EnableConfigurationProperties"
    - path: "hft-synthetic-monitoring/src/test/java/com/cryptohft/monitoring/config/MonitoringPropertiesTest.java"
      provides: "Teste de binding que valida carregamento de todos os parametros do yml"
      contains: "@SpringBootTest"
  key_links:
    - from: "MonitoringProperties.java"
      to: "application.yml"
      via: "@ConfigurationProperties(prefix = \"synthetic-monitoring\") binding"
      pattern: "synthetic-monitoring"
    - from: "SyntheticMonitoringApplication.java"
      to: "MonitoringProperties.java"
      via: "@EnableConfigurationProperties"
      pattern: "@EnableConfigurationProperties.*MonitoringProperties"
---

<objective>
Criar MonitoringProperties com @ConfigurationProperties para externalizar toda a configuracao do synthetic monitoring, e wiring no SyntheticMonitoringApplication.

Purpose: Configuracao externalizada permite ajustar intervalos, timeouts, URLs e thresholds de alerta sem recompilar. Essencial para flexibilidade em diferentes ambientes.
Output: MonitoringProperties.java com nested classes, @EnableConfigurationProperties no app, e teste de binding completo.
</objective>

<execution_context>
@/Users/devcaos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/devcaos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01-01 — SyntheticMonitoringApplication.java -->
<!-- Executor needs to ADD @EnableConfigurationProperties to existing class -->

From hft-synthetic-monitoring/src/main/java/com/cryptohft/monitoring/SyntheticMonitoringApplication.java:
```java
@SpringBootApplication
@EnableScheduling
@Slf4j
public class SyntheticMonitoringApplication {
    public static void main(String[] args) { ... }
}
```

From hft-synthetic-monitoring/src/main/resources/application.yml:
```yaml
synthetic-monitoring:
  interval-ms: 30000
  timeout-ms: 3000
  target:
    base-url: http://localhost:8080
    ws-url: ws://localhost:8080/ws/trading
  order:
    symbol: BTCUSD
    price: 100.00
    quantity: 0.001
    account: SM_CHECK
    client-id-prefix: sm-
  alert:
    consecutive-failures-threshold: 3
    warn-threshold: 5
    webhook-url: ""
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MonitoringProperties and wire to application</name>
  <files>
    hft-synthetic-monitoring/src/main/java/com/cryptohft/monitoring/config/MonitoringProperties.java
    hft-synthetic-monitoring/src/main/java/com/cryptohft/monitoring/SyntheticMonitoringApplication.java
  </files>
  <action>
    Package: `com.cryptohft.monitoring.config`

    1. CREATE `MonitoringProperties.java`:
       - Annotations: @Data (Lombok), @ConfigurationProperties(prefix = "synthetic-monitoring")
       - Usar @Data (NAO @Value) porque Spring binding precisa de setters — per CONTEXT.md design decision
       - Top-level fields:
         - `private long intervalMs = 30000;`
         - `private long timeoutMs = 3000;`
       - Nested static classes (todas com @Data):

         ```java
         @Data
         public static class Target {
             private String baseUrl = "http://localhost:8080";
             private String wsUrl = "ws://localhost:8080/ws/trading";
         }
         ```

         ```java
         @Data
         public static class Order {
             private String symbol = "BTCUSD";
             private double price = 100.00;
             private double quantity = 0.001;
             private String account = "SM_CHECK";
             private String clientIdPrefix = "sm-";
         }
         ```

         ```java
         @Data
         public static class Alert {
             private int consecutiveFailuresThreshold = 3;
             private int warnThreshold = 5;
             private String webhookUrl = "";
         }
         ```

       - Nested object fields (com inicializacao):
         - `private Target target = new Target();`
         - `private Order order = new Order();`
         - `private Alert alert = new Alert();`

    2. EDIT `SyntheticMonitoringApplication.java`:
       - ADD import: `import com.cryptohft.monitoring.config.MonitoringProperties;`
       - ADD import: `import org.springframework.boot.context.properties.EnableConfigurationProperties;`
       - ADD annotation: `@EnableConfigurationProperties(MonitoringProperties.class)` — junto com @SpringBootApplication e @EnableScheduling
       - Manter tudo que ja existe (main method, @Slf4j, etc.)
  </action>
  <verify>
    <automated>cd /Users/devcaos/conductor/workspaces/pm-trading-system/walla-walla && mvn clean compile -pl hft-synthetic-monitoring -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - MonitoringProperties.java existe com @ConfigurationProperties e 3 nested classes
    - SyntheticMonitoringApplication.java tem @EnableConfigurationProperties(MonitoringProperties.class)
    - Compila sem erros
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MonitoringProperties binding test</name>
  <files>
    hft-synthetic-monitoring/src/test/java/com/cryptohft/monitoring/config/MonitoringPropertiesTest.java
  </files>
  <action>
    Package: `com.cryptohft.monitoring.config`

    CREATE `MonitoringPropertiesTest.java`:
    - @SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT) — usa o application.yml real
    - @Autowired MonitoringProperties properties
    - Testes validando binding de TODOS os parametros do application.yml:

    1. Test `topLevelPropertiesBound()`:
       - assertThat(properties.getIntervalMs()).isEqualTo(30000)
       - assertThat(properties.getTimeoutMs()).isEqualTo(3000)

    2. Test `targetPropertiesBound()`:
       - assertThat(properties.getTarget().getBaseUrl()).isEqualTo("http://localhost:8080")
       - assertThat(properties.getTarget().getWsUrl()).isEqualTo("ws://localhost:8080/ws/trading")

    3. Test `orderPropertiesBound()`:
       - assertThat(properties.getOrder().getSymbol()).isEqualTo("BTCUSD")
       - assertThat(properties.getOrder().getPrice()).isEqualTo(100.00)
       - assertThat(properties.getOrder().getQuantity()).isEqualTo(0.001)
       - assertThat(properties.getOrder().getAccount()).isEqualTo("SM_CHECK")
       - assertThat(properties.getOrder().getClientIdPrefix()).isEqualTo("sm-")

    4. Test `alertPropertiesBound()`:
       - assertThat(properties.getAlert().getConsecutiveFailuresThreshold()).isEqualTo(3)
       - assertThat(properties.getAlert().getWarnThreshold()).isEqualTo(5)
       - assertThat(properties.getAlert().getWebhookUrl()).isEmpty()

    Usar assertj (AssertJ) para os asserts — ja vem com spring-boot-starter-test.
  </action>
  <verify>
    <automated>cd /Users/devcaos/conductor/workspaces/pm-trading-system/walla-walla && mvn test -pl hft-synthetic-monitoring -Dtest="com.cryptohft.monitoring.config.MonitoringPropertiesTest" -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    - MonitoringPropertiesTest.java existe com 4 testes
    - Todos os 4 testes passam validando binding de todos os campos
    - Teste prova que application.yml esta corretamente mapeado para MonitoringProperties
  </done>
</task>

</tasks>

<verification>
1. `mvn clean compile -pl hft-synthetic-monitoring` — compila com MonitoringProperties
2. `mvn test -pl hft-synthetic-monitoring -Dtest="com.cryptohft.monitoring.config.MonitoringPropertiesTest"` — 4 testes passam
3. SyntheticMonitoringApplication.java contem @EnableConfigurationProperties(MonitoringProperties.class)
4. Todos os parametros do bloco synthetic-monitoring.* do application.yml sao validados pelo teste
</verification>

<success_criteria>
- MonitoringProperties com @ConfigurationProperties e 3 nested classes (@Data) existe
- @EnableConfigurationProperties wired no SyntheticMonitoringApplication
- Defaults corretos: intervalMs=30000, timeoutMs=3000, consecutiveFailuresThreshold=3, warnThreshold=5
- Teste de binding valida TODOS os campos: top-level, target, order, alert
- 4 testes passam
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
